<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
    Temporary solution to allow cleartext traffic (HTTP) for local development purposes.
    REMOVE or UPDATE this configuration before deploying to production to ensure secure communication (HTTPS).
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>